---
description: Zotero 多视图阅读器插件核心功能需求
alwaysApply: false
---
# Zotero 多视图阅读器插件 - 项目需求文档

## 项目概述

开发一个 Zotero 插件，在 PDF 阅读器界面内部实现多 PDF 分屏阅读功能，支持不同 PDF 文件在多个视图中同时显示，并提供同步滚动等增强功能。

## 核心功能需求

### 1. 多 PDF 分屏阅读

**需求描述：**
- 在已打开的 PDF 阅读器界面内部实现分屏阅读功能（而非在工具栏或标签页层面）
- 在已打开的 PDF 中右键点击时，弹出分屏选项菜单，用户可以选择创建新的分屏视图
- 与 Zotero 原生的 split view 不同，每个视图可以显示**不同的 PDF 文件**
- 不局限于 2 个视图，支持多个视图同时显示

**实现要点：**
- **触发方式**：在已打开的 PDF 阅读器界面中右键点击，弹出上下文菜单，提供分屏选项（如"创建新分屏视图"、"添加到分屏视图"等）
- 参考 Zotero 原生 split-view 的 DOM 结构实现：
  - 使用外层容器 `<div id="split-view">` 包裹多个视图
  - 每个视图使用独立的容器（如 `<div class="view">`），内部包含 PDF.js viewer 的 iframe
  - 每个 iframe 独立加载不同的 PDF 文件
- 每个视图独立管理自己的 PDF 文件
- 视图数量可动态增加/减少
- 参考效果：https://hjfy.top/arxiv/2503.11651（仅参考分屏布局效果，不考虑翻译功能）

### 2. 视图布局切换

**需求描述：**
- 视图之间可以切换布局方式
- 仅支持左右分屏布局

**实现要点：**
- 提供布局切换 UI 控件（用于调整左右视图的分割比例）
- 支持动态调整视图大小和位置（拖拽分割线）
- 布局状态可保存和恢复

### 3. 鼠标同步滚动控制

**需求描述：**
- 每个视图可以独立开启/关闭同步滚动功能
- 当某个视图开启同步滚动时，该视图成为"主控制视图"
- 鼠标光标在开启同步滚动的视图内时：
  - 使用鼠标滚轮滚动页面
  - 其他开启同步滚动的视图也会同步滚动
- 鼠标光标移动到其他视图时：
  - 仅滚动当前光标所在的视图内容
  - 不影响其他视图

**实现要点：**
- 需要监听鼠标滚轮事件
- 跟踪鼠标光标位置和当前激活的视图
- 实现视图间的滚动位置同步算法
- 提供 UI 指示器显示哪些视图开启了同步滚动

### 4. 标签页集成功能

**需求描述：**
- 在 PDF 阅读器界面内部，每个视图可以有自己的标签页栏（在视图内部实现，而非 Zotero 主界面的标签页）
- 在已打开的 PDF 中右键点击时，可以弹出菜单项："Add tab to new split view" 或类似选项
- 每个视图的标签页可以独立切换显示不同的 PDF 文件

**实现要点：**
- 在 PDF 阅读器界面内部实现标签页功能（参考原生 split-view 的结构）
- 通过右键菜单触发分屏功能，用户可以选择将当前 PDF 添加到新的分屏视图
- 每个视图维护独立的标签页列表
- 支持在视图间拖拽标签页切换文件
- 标签页状态与视图绑定

## 技术实现考虑

### 架构设计
- 在 PDF 阅读器界面内部实现分屏，而非在 Zotero 工具栏或标签页层面
- **右键菜单集成**：监听 PDF 阅读器界面的右键事件，注入自定义菜单项，提供分屏选项
- 参考 Zotero 原生 split-view 的 DOM 结构：
  - 创建 `<div id="split-view">` 容器作为外层包裹
  - 在容器内动态创建多个视图容器，每个视图包含独立的 PDF.js viewer iframe
  - 每个 iframe 可以加载不同的 PDF 文件（通过修改 iframe 的 src 或使用 postMessage 通信）
- 管理多个视图实例的生命周期
- 实现视图间的通信机制（用于同步滚动）

### UI/UX 设计
- 参考 https://hjfy.top/arxiv/2503.11651 的分屏效果
- 保持与 Zotero 原生 UI 风格一致
- 提供清晰的视觉反馈（同步状态、激活视图等）

### 性能优化
- 多个 PDF 同时加载时的内存管理
- 滚动同步的性能优化（避免过度渲染）
- 视图切换的流畅性

## 开发优先级建议

1. **Phase 1**: 基础多视图框架
   - 实现多个独立视图的创建和管理
   - 每个视图可以显示不同的 PDF

2. **Phase 2**: 布局切换功能
   - 实现视图布局的切换和调整

3. **Phase 3**: 同步滚动功能
   - 实现鼠标同步滚动控制

4. **Phase 4**: 标签页集成
   - 集成到 Zotero 标签页系统
   - 实现视图内的标签页管理

## 注意事项

- 需要深入研究 Zotero 的 PDF 阅读器 API
- 确保插件与 Zotero 的更新兼容
- 考虑不同操作系统（Windows、macOS、Linux）的兼容性
- 注意内存占用，特别是同时打开多个大型 PDF 时